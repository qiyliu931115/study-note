栈中的数据都是栈帧（stack frame）的格式存在。

先进后出/后进先出 压栈push 出栈pop

当前执行的栈顶栈帧 当前栈帧对应的方法是当前方法 定义这个方法的类就是当前类

该方法调用其他方法入栈 那其他方法就处于栈顶 成为当前栈帧

不同线程的栈帧不允许互相引用的 栈是私用的
（线程之间不允许，如果是进程之间，堆外内存是可以共享的，元空间就在堆外内存）

Java方法两种返回函数的方法 一是正常的return 二是抛出异常。

这两种都会导致栈弹出。

栈帧的内部结构：

    1 局部变量表(local variables)
    
    2 方法返回地址(return address)（方法正常退出或者异常退出的定义）
    
    3 操作数栈(operand stack)（表达式栈）
    
    4 动态链接(dynamic linking) (指向运行时常量池的方法引用)
    
    5 附件信息

